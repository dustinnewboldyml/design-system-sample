import { LitElement } from "lit";

/**
 * This is utilized for components or modules where the component may have child components
 */
export class Component extends LitElement {
    // protected createRenderRoot() {
	// 	return this
	// }
}